class Component:
    # sets the hardware components based on user input parameters.
    def setComponent(cpu, memory, ram, GPU = None):
        memory = memory
        cpu = cpu
        ram = ram
        GPU = GPU
    # Funciton should be able to identify the product of the hardware components
    # maybe a ml model and/or database of hardware components.
    # Future Feature: if the user does not specify model/brand of component assume,
    #   stock product. 
    def hardware_specifics():
        pass
    
